/* SPDX-License-Identifier: GPL-2.0-or-later */
/* Test-only hooks for the bundled movement differential. */
void DK_WeaponMoveZig(pmove_t *move, int msec);
int DK_Attribute(const playerState_t *state, int attribute, int time);
